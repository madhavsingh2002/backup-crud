const Util = {
    emailRegex: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
    passwordRegex:/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&#]{8,}$/,
    zero:0,
    three:3,
    ten:10,
    twelve:12,
    eight:8,
    twohud:200
}

export default Util;
